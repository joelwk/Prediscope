# xAI Grok Configuration
# Get your API key from https://console.x.ai
XAI_API_KEY=your_xai_api_key_here
GROK_MODEL=grok-4-1-fast-reasoning

# PredictBase API Configuration
# API base URL for PredictBase
PREDICTBASE_API_BASE_URL=https://api.predictbase.app
# Optional: API key if required by PredictBase
PREDICTBASE_API_KEY=your_predictbase_api_key_here
# Header name for API key (default: x-api-key)
PREDICTBASE_API_KEY_HEADER=x-api-key
# Prefix for API key (empty for x-api-key header)
PREDICTBASE_API_KEY_PREFIX=

# Base Network Configuration (Chain ID 8453)
# Get your Alchemy RPC URL from https://www.alchemy.com
ALCHEMY_RPC_URL=https://base-mainnet.g.alchemy.com/v2/your_alchemy_api_key
# Your wallet private key (keep secure!)
WALLET_PRIVATE_KEY=your_wallet_private_key_here
# Base mainnet chain ID
CHAIN_ID=8453
# USDC token address on Base mainnet
USDC_TOKEN_ADDRESS=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913
# PredictBase contract address (set when known)
PREDICTBASE_CONTRACT_ADDRESS=0xaeA742f80922f7C94B8FD91686c9dFbDFE90d9E6

# Risk Management Configuration
HIGH_CONFIDENCE_POSITION_OVERRIDE=0.8
# Minimum confidence threshold (0.0 to 1.0) to execute trades
MIN_CONFIDENCE=0.50
# Minimum market liquidity in USDC to consider
MIN_LIQUIDITY_USDC=15.0
# Polling interval in seconds (how often to check markets)
POLL_INTERVAL_SEC=300

# Edge gating for low-priced outcomes
# Outcomes below this probability face a higher edge requirement
LOW_PRICE_THRESHOLD=0.50
# Minimum edge required for outcomes below LOW_PRICE_THRESHOLD
LOW_PRICE_MIN_EDGE=0.08
# Near-coinflip zone: prices in this band require elevated edge (uses LOW_PRICE_MIN_EDGE)
COINFLIP_PRICE_LOWER=0.45
COINFLIP_PRICE_UPPER=0.55
# Minimum edge when edge_source=fallback (no external odds found)
FALLBACK_EDGE_MIN_EDGE=0.08

# Market Filtering
# Comma-separated list of allowed categories (empty = all)
MARKET_CATEGORIES_ALLOWLIST=
# Comma-separated list of blocked categories
MARKET_CATEGORIES_BLOCKLIST=

# Execution Mode
# Set to false to execute real trades (use with caution!)
DRY_RUN=false
# Automatically approve USDC spending when needed
AUTO_APPROVE_USDC=true
# Execute transactions on-chain (requires EXECUTE_ONCHAIN=true)
EXECUTE_ONCHAIN=true

# USDC Configuration
# Decimals for USDC (6 for standard USDC)
USDC_DECIMALS=6

# Bet sizing
MIN_BET_USDC=2
MAX_BET_USDC=4

# Position limits
# Max single-market exposure as fraction of available bankroll (0.25 = 25%)
MAX_POSITION_PCT_OF_BANKROLL=0.25
# Ensure position cap still allows at least one MIN_BET_USDC order
POSITION_CAP_FLOOR_TO_MIN_BET=true

# Only bet on markets closing between 1-7 days from now
MARKET_MIN_CLOSE_DAYS=0
MARKET_MAX_CLOSE_DAYS=30

# Score gate mode: off | shadow | active
# Recommended production setting after validation: active
SCORE_GATE_MODE=active
# Cap how much evidence_quality can inflate score on sports markets
SCORE_MAX_EVIDENCE_QUALITY_SPORTS=0.70

# Parallel analysis
PARALLEL_ANALYSIS_ENABLED=true
ANALYSIS_MAX_WORKERS=5

# Phase 3 (post-fix): Bayesian + LMSR + Kelly
BAYESIAN_ENABLED=true
# Keep Bayesian tracking on, but do not let it override sports confidence
# until the sports segment is calibrated.
BAYESIAN_APPLY_TO_SPORTS=false
# Skip Bayesian update count when reasoning hash hasn't changed between cycles
BAYESIAN_SKIP_STALE_UPDATES=true
LMSR_ENABLED=true
KELLY_SIZING_ENABLED=true

# Bayesian
BAYESIAN_PRIOR_DEFAULT=0.50
BAYESIAN_MIN_UPDATES_FOR_TRADE=2
# For markets closing soon, allow faster posterior activation
BAYESIAN_SHORT_HORIZON_HOURS=24
BAYESIAN_MIN_UPDATES_SHORT_HORIZON=1

# LMSR (execution-aware gate)
LMSR_LIQUIDITY_PARAM_B=100000
LMSR_MIN_INEFFICIENCY=0.03

# Kelly
KELLY_FRACTION_DEFAULT=0.25
KELLY_FRACTION_SHORT_HORIZON_HOURS=1
KELLY_FRACTION_SHORT_HORIZON=0.10
# Allow stronger first entry on fresh positions when edge is high
STRONG_EDGE_INITIAL_ENTRY_ENABLED=true
STRONG_EDGE_INITIAL_ENTRY_MIN_EDGE=0.05
STRONG_EDGE_INITIAL_ENTRY_BET_PCT=1.0
KELLY_ALLOW_MIN_BET_FLOOR_UPSCALE=false
# Kelly min-bet handling policy: skip | floor | fallback_edge_scaling
# fallback_edge_scaling preserves Kelly as primary sizing and falls back to
# edge-scaling sizing when Kelly is below MIN_BET_USDC.
KELLY_MIN_BET_POLICY=fallback_edge_scaling

# Sports accuracy guardrails
# Require stronger base model confidence before any confidence amplification.
SPORTS_MIN_MODEL_CONFIDENCE=0.66
# Require consensus edge from external odds for sports picks.
SPORTS_REQUIRE_EXTERNAL_EDGE=true
SPORTS_MIN_EXTERNAL_EDGE=0.04

# Throughput / cadence
REANALYSIS_COOLDOWN_HOURS=2
URGENT_REANALYSIS_COOLDOWN_HOURS=0
URGENT_REANALYSIS_DAYS_BEFORE_CLOSE=1

# Optional: turn off outside calibration sessions
CALIBRATION_MODE_ENABLED=false